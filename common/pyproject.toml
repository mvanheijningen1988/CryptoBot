[project]
name = "cryptobot-common"
version = "1.0.0"
description = "Shared models, strategies, and exchange adapters for CryptoBot"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.9,<3",
    "websocket-client>=1.8,<2",
]
